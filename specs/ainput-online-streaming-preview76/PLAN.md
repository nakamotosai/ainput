# Plan

1. Add online mode/config schema and default TOML rendering.
2. Add tray menu item and mode switching for online streaming.
3. Add online worker entry that uses `[voice.online_streaming]` and disables local rewrite/GPU paths.
4. Add online release policy: HUD snapshot first, background finish after paste.
5. Update default config/docs/version, package preview76, launch and verify.
